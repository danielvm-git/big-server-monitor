session:
  project: bigservermonitor
  started: "2026-06-11"
  status: building

active:
  release: 2.0.0
  flow: release
  epic: all
  phase: RELEASE
  branch: feat/swift6-rewrite

release:
  pr: "https://github.com/danielvm-git/big-server-monitor/pull/1"
  pr_opened: "2026-06-11"
  pending: "merge PR, then tag v2.0.0 on main (release.last_tag)"

metrics:
  story_start: "2026-06-11T00:00:00Z"

history:
  - release: 1.0.0
    note: >
      PortKeeper v1 (Go ECC kernel + Wails v2 + React 19) completed 2026-06-09/10:
      all 6 components, 150+ tests, Playwright e2e, structured slog logging.
      Superseded by the 2.0.0 Swift 6 rewrite (see release-plan.yaml).
  - release: 2.0.0
    epic: e01-scaffold-and-brand
    status: committed
    commit: 2b53c9c
  - release: 2.0.0
    epic: e02-process-monitor-core
    status: committed
    commit: b03b71d
  - release: 2.0.0
    epic: e03-popover-ui
    status: committed
    commit: edfd789
  - release: 2.0.0
    epic: e04-health-activity-logs
    status: committed
    commit: f42179f
  - release: 2.0.0
    epic: e05-settings-notifications
    status: committed
    commit: multiple (SettingsStore, JSONLogger, Notifier, SettingsSheet, AppState wiring)
  - release: 2.0.0
    epic: e06-cutover
    status: committed
    commit: multiple (legacy removal, docs refresh)

handoff: >
  VERIFY phase complete 2026-06-11. Tests 53/53, BUILD OK.
  M1 (menubar icon + popover): PASSED.
  M2 (server interaction): PASSED — two fixes during UAT:
    (a) chevron expand + labeled buttons (4776913),
    (b) server list collapsed to zero height: LazyVStack/flexible ScrollView
        get zero proposed height in MenuBarExtra sizing pass; eager VStack +
        explicit height from row count (a91bfc1).
  M3 (health/activity/logs/settings): PASSED — two fixes during UAT (addfe0c):
    (a) LogCapture had no feed; now pulls unified log (`log show`) per PID
        on Logs open, per specs/005 v1 design.
    (b) sheets attached to MenuBarExtra popover died on first click;
        converted to standalone Window scenes + NSApp.activate (LSUIElement).
  M4 (cutover): PASSED automated checks (no .go/.tsx, tests green).
  Known limitation: notification permission fails on unsigned debug builds;
  recheck crash alerts on signed Release build.
  RELEASE: branch + main pushed to github.com/danielvm-git/big-server-monitor;
  PR #1 open. After merge: tag v2.0.0, set release.last_tag, archive this flow.
